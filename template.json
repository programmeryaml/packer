{
  "variables": {
    "ansible_playbook_path": "",
    "aws_ami_name": "-{{timestamp}}",
    "aws_region": ""
  },
  "builders": [
    {
      "type": "amazon-ebs",
      "region": "{{user `aws_region`}}",
      "source_ami": "ubuntu/images/hvm-ssd/ubuntu-jammy-22.04-amd64-server-*",
      "instance_type": "t2.micro",
      "ssh_username": "ubuntu",
      "ami_name": "{{user `aws_ami_name`}}"
    }
  ]
}
